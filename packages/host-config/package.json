{
  "name": "@emdzej/ediabasx-host-config",
  "version": "0.5.0",
  "description": "Shared loader for ~/.config/ediabasx/config.json + interface-selection resolver. Consumed by @emdzej/ediabasx-cli and external CLIs that drive ediabasx (e.g. nfsx) so the config schema + override rules stay in one place.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run --passWithNoTests",
    "lint": "eslint .",
    "typecheck": "tsc --noEmit",
    "dev": "tsc -p tsconfig.json -w --preserveWatchOutput"
  },
  "dependencies": {
    "@emdzej/ediabasx-interfaces": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^22.0.0"
  },
  "publishConfig": {
    "access": "public"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/emdzej/ediabasx.git"
  }
}
