{
  "name": "@ediabasx/ediabas",
  "version": "0.1.0",
  "private": true,
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run --passWithNoTests",
    "lint": "eslint .",
    "typecheck": "tsc --noEmit",
    "dev": "tsc -p tsconfig.json -w --preserveWatchOutput"
  },
  "dependencies": {
    "@ediabasx/best-parser": "workspace:*",
    "@ediabasx/core": "workspace:*",
    "@ediabasx/interpreter": "workspace:*",
    "@ediabasx/interface-base": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "vitest": "^3.0.5"
  }
}
