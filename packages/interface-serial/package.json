{
  "name": "@ediabasx/interface-serial",
  "version": "0.1.0",
  "private": true,
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run --passWithNoTests",
    "lint": "eslint .",
    "typecheck": "tsc --noEmit",
    "dev": "tsc -p tsconfig.json -w --preserveWatchOutput"
  },
  "dependencies": {
    "@ediabasx/core": "workspace:*",
    "@ediabasx/interface-base": "workspace:*",
    "@ediabasx/protocol-uds": "workspace:*",
    "serialport": "^12.0.0"
  }
}
